---
title: opendetail doctor
description: Check config, built index, route file (self-hosted), and required environment variables for your integration mode.
---

`opendetail doctor` runs a **checklist** and prints ✓ or ✗ for each item. It exits with an error if anything required is missing.

## Usage

<CodeBlock lang="bash">{`opendetail doctor [--integration <self-hosted|hosted>] [--config <path>] [--route <path>] [--cwd <path>]
`}</CodeBlock>

## Self-hosted expectations

- **Config:** `opendetail.toml` (or `--config`) exists.
- **Index:** `.opendetail/index.json` (or configured output) exists.
- **Route:** the API route file exists at the configured path.
- **Env:** `OPENAI_API_KEY` is set where the route runs.

## Hosted expectations

- **Config** and **index** as above.
- **Env:** `OPENDETAIL_ENDPOINT` points at your hosted OpenDetail HTTP API.

## Examples

<CopyCommand bun="bunx opendetail doctor" npm="npx opendetail doctor" />

<CopyCommand bun="bunx opendetail doctor --integration hosted" npm="npx opendetail doctor --integration hosted" />

## Related

- [`setup`](/docs/cli/setup) if files are missing.
- [`build`](/docs/cli/build) if the index is stale or absent.
