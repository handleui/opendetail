import type { SiteNavSection } from "@/lib/site-nav";

/** Nested panel under `/ui` — the primary entry is the root row link; no extra tree. */
export function getUiSidebarSections(): SiteNavSection[] {
  return [];
}
