# Configuration

## base_path

Use `base_path` to prepend a route prefix to generated URLs.

## include

The `include` array controls which markdown and MDX files are indexed.

## exclude

Use `exclude` to remove matched files from indexing.
