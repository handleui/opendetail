import Demo from "./demo"

export const meta = { section: "Guides" }

# Composition

Use the `<Demo />` component to show examples.

<Demo>
  Inline MDX content still matters for the extracted text.
</Demo>

## Server Runtime

`createOpenDetail` returns an assistant.

<Callout title="Server only">The runtime stays on the server.</Callout>
