# opendetail-client

Framework-agnostic NDJSON transport for OpenDetail.

## Exports

- `createOpenDetailClient`
- transport status, request, stream event, and error types

This package owns the network/client transport only. It does not depend on React or any UI layer.
